<!DOCTYPE html>
<html
  lang="<%= sys_language %>"
  dir="<%= sys_dir %>"
>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= sys_company_name %></title>
    <style><%= sys_style %></style>
<!--
    <style>
      /* general reset */
      body, table, td, a {
        font-family: Arial, sans-serif;
        text-size-adjust: 100%;
      }
      table, td {
        border-collapse: collapse;
      }
      img {
        border: 0;
        height: auto;
        line-height: 100%;
        outline: none;
        text-decoration: none;
      }
      body {
        margin: 0;
        padding: 0;
        width: 100% !important;
        -webkit-font-smoothing: antialiased;
        background-color: #f4f4f4;
      }
      /* container */
      .email-container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
      }
      /* header */
      .email-header {
        background-color: #1f2937;
        padding: 20px;
        text-align: center;
      }
      .email-header img {
        max-width: 150px; /* adjust as needed */
        height: auto;
        margin-bottom: 10px;
      }
      .email-header h1 {
        margin: 0;
        color: #ffffff;
        font-size: 24px;
        font-weight: normal;
      }
      /* body */
      .email-body {
        padding: 20px;
      }
      .email-body h2 {
        font-size: 20px;
        color: #333333;
        margin: 0 0 10px;
      }
      .email-body p {
        font-size: 16px;
        line-height: 1.5;
        color: #666666;
        margin: 0 0 20px;
      }
      .email-body a {
        color: #1f2937;
        text-decoration: none;
        font-weight: bold;
      }
      /* button */
      .email-button {
        display: block;
        width: max-content;
        background-color: #1f2937;
        color: #ffffff;
        padding: 12px 20px;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        margin: 20px auto 0;
        font-size: 16px;
      }
      /* footer */
      .email-footer {
        background-color: #1f2937;
        color: #dddddd;
        padding: 20px;
        text-align: center;
        font-size: 12px;
      }
      .email-footer a {
        color: #dddddd;
        text-decoration: underline;
      }
    </style>
  -->
    </head>

  <body>
    <table role="presentation" class="email-container" width="100%" cellpadding="0" cellspacing="0">
      <tr>
        <td class="email-header">
          <!-- logo image -->
          <img src="<%= sys_company_logo %>" alt="<%= sys_company_name %> logo">
          <h1><%= sys_company_title %></h1>
        </td>
      </tr>
      <tr>
        <td class="email-body">
          <p><%= t("Hello") %>, <b><%= userFirstName %> <%= userLastName %></b>.</p>
          <p></p>
          <p><%= t("The code to verify your registration is") %></p>
          <p></p>
          <p style="font-size: 24px; text-align: center;"><b><%= signupVerificationCode %></b></p>
          <p></p>
          <p><i><%= t("If you did not request it, please ignore this email") %>.</i>.</p>
          <p><i>
            <%= t("If you have any questions, feel free to") %>
            <a href="<%= sys_company_mailto %>"><%= t("contact us") %></a>
            <%= t("at any time") %>.
          </i></p>
        </td>
      </tr>
      <tr>
        <td class="email-footer">
          <p><%= sys_company_copyright %>. <%= t("All rights reserved") %>.</p>
        </td>
      </tr>
    </table>
  </body>
</html>